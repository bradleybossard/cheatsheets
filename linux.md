    whatis <application-name>  # Describes the application
    ls -l /proc/<pid>/cwd      # Determine current working directory of process
    lsof                       # List open files
    ps -aux                    # List all processes
    df                         # Report file system disk usage
