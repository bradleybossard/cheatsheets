```
nc -zv <hostname> <port>                      # Check the connection of port
nc -zv <hostname> <start_port>-<end_port>     # Check the connection of port range
                                              # where -z simply scans without sending data
                                              # -v verbose mode
```
